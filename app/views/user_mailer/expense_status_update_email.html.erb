<p class="font-bold text-xl">Your expense entry has 
  <span class="<%= @expense.status_before_type_cast == 1 ? "text-primary" : "has-text-danger" %>">
    <%= @expense.status %>
  </span>
  been by the admin: <%= @admin.email %></p>
<div class="text-lg font-medium">
  <%= render "expense" %>
</div>
<% if @expense.status == "rejected" %>
  <p>If you have any questions or concerns, please contact the admin.</p>
<% end %>
