function inviteUser () {  
  const emailInput = document.querySelector("#invite_user-form #email-input");
  const submitButton = document.querySelector("#invite_user-form button[type=submit]");

  submitButton.addEventListener("click", (e) => {
    e.preventDefault();
    
  });
}

inviteUser();
