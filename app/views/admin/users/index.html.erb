<section class="flex flex-col h-full overflow-hidden gap-5">
  <span class="flex items-center justify-between">
    <h2 class="font-bold text-4xl">User Management</h2>
    <div id="invite_user-container">
      <%= render "button" %>
    </div>
  </span>
  <div id="add_user_form-container">
  </div>
  <div class="flex flex-col p-2 h-full overflow-y-auto" id="users_list">
    <%= render @users %>
  </div>
</section>
