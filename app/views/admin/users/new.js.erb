$(document).ready(function() {
  const addUserFormContainer = $("#add_user_form-container");
  const formToggleButton = $("#form_toggle-button");
  
  addUserFormContainer.empty().append("<%= j render 'form') %>");
  formToggleButton.hide();
});
