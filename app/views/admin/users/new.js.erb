function insertForm() {
  const button = document.getElementById("form-toggle_button");
  button.insertAdjacentHTML("afterend", "<%= escape_javascript( render partial: "form" ) %>");
  button.remove();
}
insertForm();
