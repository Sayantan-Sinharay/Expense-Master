$(document).ready(function() {
  function insertForm() {
    const button = $("#form-toggle_button");
    button.after("<%= escape_javascript(render partial: 'form') %>");
    button.hide();
  }
  insertForm();
});
