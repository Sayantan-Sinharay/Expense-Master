$(document).ready(function () {
  $('#category_<%= @category.id %> .accordion-content').show(function () {
    $("#category_<%= @category.id %> #subcategory_list").append('<%= j render "form" %>');
    $("#category_<%= @category.id %> #subcategory-input").focus();
  });
});
