$(document).ready(function() {
  $("#category_list").prepend('<%= j render @category %>');
  $('#category_<%= @category.id %> .accordion-content').hide();
  $("#create_category-form")[0].reset();
  $('#flash').html('<%= j render "shared/flashes" %>');
});