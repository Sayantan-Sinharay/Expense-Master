$(document).ready(function() {
  $("#category_list").append('<%= j render @category %>');
  $('#category_<%= @category.id %> .accordion-content').hide();
  $("#create_category-form")[0].reset();
  $('#flash-container').html('<%= j render "shared/flashes" %>');
});