<section class="flex flex-col h-full overflow-hidden gap-5">
  <%= render "shared/back_button" %>
  <h2 class="font-bold text-2xl">Notifications</h2>
  <div id="notifications-container" class="flex flex-col p-2 h-full overflow-y-auto">
    <%= render @notifications %>
  </div>
</section>
<%= javascript_pack_tag "notifications/index" %>
